CREATE DATABASE tarefas_app;
USE tarefas_app;

CREATE TABLE usuarios (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100),
    email VARCHAR(100) UNIQUE,
    senha VARCHAR(255)
);

CREATE TABLE tarefas (
    id INT AUTO_INCREMENT PRIMARY KEY,
    usuario_id INT,
    titulo VARCHAR(255),
    horario TIME,
    concluida BOOLEAN DEFAULT FALSE,
    data DATE,
    prioridade ENUM('importante','trivial') DEFAULT 'trivial',
    descricao TEXT,
    FOREIGN KEY (usuario_id) REFERENCES usuarios(id) ON DELETE CASCADE
);

CREATE TABLE configuracoes (

    id INT PRIMARY KEY AUTO_INCREMENT,
    usuario_id INT NOT NULL,
    limite_visual TINYINT(1) DEFAULT 0,
    quantidade_tarefas INT DEFAULT 3,

    FOREIGN KEY (usuario_id)
    REFERENCES usuarios(id)
    ON DELETE CASCADE
);

CREATE TABLE fases (

    id INT AUTO_INCREMENT PRIMARY KEY,

    nome VARCHAR(50),

    quantidade INT NOT NULL
);

INSERT INTO fases (nome, quantidade) VALUES
('Fase 1', 5),
('Fase 2', 10),
('Fase 3', 30),
('Fase 4', 50),
('Fase 5', 100);

ALTER TABLE usuarios
ADD tarefas_concluidas INT DEFAULT 0;

ALTER TABLE usuarios 
ADD COLUMN reset_token VARCHAR(64) NULL AFTER senha,
ADD COLUMN token_expira DATETIME NULL AFTER reset_token;

CREATE TABLE IF NOT EXISTS lembretes (
  id INT AUTO_INCREMENT PRIMARY KEY,
  usuario_id INT NOT NULL,
  titulo VARCHAR(255) NOT NULL,
  prioridade VARCHAR(20) DEFAULT 'trivial',
  data_criacao TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (usuario_id) REFERENCES usuarios(id) ON DELETE CASCADE
);



	
